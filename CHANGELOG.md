# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Fixed 修复

- **`加入抽奖` / `退出抽奖` 用不存在的编号会抛内部异常**：改为给出「没有找到该抽奖！」提示
- **没有 binding 的用户走指令加入会抛内部异常**：统一用 `resolveAid` 解析（带重试），取不到时提示稍后再试；
  关键词路径与指令路径现在共用同一套兜底
- **已开奖的抽奖仍能被加入**：`giveaway/roll-join` 现在校验抽奖是否存在与是否已开奖（口令与指令两条路径统一拦截）；
  同时**开奖后不再允许退出**（名单已确定，避免中奖名单与开奖结果不一致）
- **参与人数少于奖品数时「不允许重复中奖」失效**：`uniqueWinners` 以前会循环复用成员（2 人 5 奖品 → 有人中 3 次），
  现在只抽 `min(参与人数, 奖品数)` 个名额，剩余奖品不发放；抽奖时还会对历史重复参与记录按 `user_id` 去重
- **重复发口令可能写入两条参与记录**：`roll_member` 现在对 `(roll_id, user_id)` 声明**唯一索引**（并发重复写入由数据库兜底），
  写入处捕获唯一约束冲突并按「已参与」提示；另外参与条件校验（要访问接口、可能耗时数秒）后增加一次写前复查，把窗口再压到一次数据库往返。
  抽奖时也会按 `user_id` 去重，历史脏数据不会导致同一个人中两次
  > 升级提示：唯一索引在启动时创建，若历史数据里已存在 `(roll_id, user_id)` 重复行会创建失败（插件仍可运行，只是少了这层保护）。
  > 需要清理可用：`DELETE FROM roll_member WHERE id NOT IN (SELECT MIN(id) FROM roll_member GROUP BY roll_id, user_id);`
- **开奖时间填成过去的时间会导致「永远不会自动开奖」**：`node-schedule` 对过去的时间点返回 `null`，排期管理器却当成已排期
  （日志还显示 scheduled），之后删除/过期时 `job.cancel()` 抛异常并中断清理流程。
  现在排期管理器对 `null` 直接返回失败并告警，创建抽奖时也会拒绝过去的开奖时间（提示重新填写）
- **抽奖记录过期时漏清理 `roll_policy`**（per-roll 参与条件），现已一并删除
- **4 段式开奖时间取的是宿主机年份**（跨年时区可能差一年）：改为取目标时区的当前年
- 去掉 `抽奖` 手动开奖流程里遗留的 `console.log`

### Added 新增

- **CI（GitHub Actions）**：push / PR 时在 Node 20 / 22 上执行 `npm ci → npm run build → npm test`；
  离线用例已迁入仓库 `test/`（路径全部改为相对，`npm test` 一键跑 5 套，`npm run test:shots` 跑可选截图验收）

### Changed 变更

- **README 重写**：按当前功能重新组织（特性 / 安装 / 指令列表 / 交互详解 / 配置 / 图片渲染 / 参与条件 /
  权限 / 数据与事件 / 多语言 / 开发），并补充 CI 徽章与测试说明
- **多语言收尾**：管理员诊断指令（`抽奖接口诊断` / `抽奖成员诊断`）的输出全部改为 i18n（三语），
  不再硬编码中文；创建流程的取消词扩展为 `q` / `quit` / `cancel` / `取消` / `abbrechen`
- **新增功能总览文档** `docs/features-and-interactions.md`：全部指令、交互流程、配置面、数据模型、
  事件、多语言覆盖与 1.0 发布条件评估

## [0.1.3] - 2026-09-16

### Changed 变更

- **`gothic`（哥特）风格合并到 `avemujica`**：两套配色气质重复（都是暗色 + 衬线 + 古金 + 罗马数字名次），
  现在只保留 `avemujica` 一套（衬线字体 / 古金描边 / 纹章装饰本来就是它的特征）
  - **枚举值直接删除、不做兼容**：`render.style` 只剩 `default` / `anime` / `avemujica`，
    控制台里不再出现哥特；老配置里写着的 `gothic` 会在配置校验阶段被拒绝（升级后重新选一次风格即可，
    这属于有意的破坏性变更 —— 该风格是本次新增、尚未随 npm 发布过）
  - 三语控制台描述更新为三种风格 + 删除说明；主题 CSS 少一份，包体更小

### Added 新增

- **`抽奖详情 <编号>` 支持图片**：装了 `koishi-plugin-puppeteer` 后，详情会用**与创建成功时同一张卡片**重新渲染
  （状态胶囊「进行中 / 已结束」+ 编号 + 参与人数，奖品与参与条件照旧；**已开奖的抽奖附中奖名单**：名次 + 头像 + 昵称 + 奖品），
  渲染失败或未装 puppeteer 时回退为原来的文字详情
  - 新增开关 `render.detail`（默认开启），可单独关掉详情出图（详情比创建结果发得更频繁，方便按群习惯取舍）
  - 卡片构建代码抽成公共小块（信息行 / 奖品区块 / 参与条件区块 / 人员行），创建 / 详情 / 成员 / 开奖四张卡片共用
- **参与名单（进行中的抽奖也能看到谁参加了）**：详情卡片与开奖卡片新增「参与名单」区块，`抽奖成员 <编号>` 也可用图片渲染，
  每行是 **头像 + 昵称 + QQ 号**（头像优先取 OneBot 资料，取不到按 QQ 号拼 qlogo；昵称取不到退化为 QQ 号）
  - 新增开关 `render.member`（默认开启，控制 `抽奖成员` 是否出图）
  - 新增 `render.memberLimit`（1~100，默认 **12**）：**图片里最多显示多少位参与者**，超出只显示前 N 个并在末尾提示「…… 等共 X 人」；
    详情卡片的参与名单共用这个上限（`抽奖成员` 想看完整名单把它调大即可）

### Fixed 修复

- **图片两侧大片背景（暗色主题下像"黑边"）、群里字体显示偏小**：`koishi-plugin-puppeteer` 截取的是 `body` 包围盒，
  而旧样式用 `display: flex` 让 `body` 铺满浏览器视口（插件默认视口 1280px），卡片本身只有 760px
  → 截图 2560px 宽、两侧各约 260px 背景色，群里按聊天窗口缩放后字体更小。
  现在 `body` 收缩到卡片大小（`display: inline-block`，只留 14px 内边距），卡片默认宽度 **420px**
  （新增 `render.width`，可调 320~900），图片宽度 = 卡片宽度 + 内边距，与浏览器视口无关
- **文字版参与名单 / 详情中奖名单在取不到用户资料时会整个失败**：`rollMemberMsgFromRoll` 直接 `user.name`、
  `rollDetailMsgFromRoll` 直接 `userPlatformId[0].platform` 且不判空 —— binding 缺失、bot 平台不匹配或 `getUser` 抛错时
  整条名单发不出去。现在统一走 `collectWinners` / 兜底逻辑，取不到资料退化为只显示 QQ 号，单个用户资料缺失不再影响整条消息
- **退出抽奖的文案键名错误**：代码用 `events.roll.quit.*`，三语文件里却只有 `events.roll.end.*` → 群里退出抽奖时收到的是原始 key，
  现已把三语键改名对齐（新增的 i18n 用例会扫描代码里引用的文案键，防止再漏）
- **`抽奖详情` 在没有 locales 数据的会话里会抛错**：`getCurrentLocales` 直接读 `session.user.locales.length`，
  未装 locales 插件的平台上是 `undefined` → 现在兜底成空数组（详情指令此前就有这个隐患）
- 新增真实 Chromium 截图用例（`shot-test`，14 条）：三种卡片 × 四套主题逐一断言「无两侧大片背景」「图片宽度 = 卡片宽度 × 2 DPI」，
  并校验 `render.width` 生效 —— 这类回归以后能被直接测出来

## [0.1.2] - 2026-09-15

### Added 新增

- **图片渲染（可选依赖 puppeteer）**：安装并启用 `koishi-plugin-puppeteer` 后，
  **创建成功提示、抽奖列表、开奖结果**都会用内置 HTML 模板渲染成图片
  - 配置新增 `render` 组：`create`（创建后的抽奖内容卡片）、`list`（列表出图）、`result`（开奖结果出图）、
    `avatar`（结果图里显示中奖者头像与昵称），默认都开启
  - 中奖者头像优先取 OneBot 用户资料，取不到按 QQ 号拼 qlogo；昵称取不到退化显示 QQ 号，头像加载失败显示首字
  - 未安装 / 未启用 / 渲染失败时**自动回退为文字消息**，只打一条 warn 日志，不影响开奖与列表逻辑
  - **创建结果：出图时只用图片**（编号 / 口令 / 奖品 / 参与条件都在卡片里），不再重发文字成功提示；
    渲染失败或未装 puppeteer 时回退为文字
  - 开奖消息仍保留一条带真实 `@中奖者` 的文字行（图片无法提醒到人），图片附在其后
  - 顺带加固：文本版开奖消息在适配器没有 `getUser` / 没有 binding 时不再抛错，退化为只显示 QQ 号
  - 图片标签跟随语言（zh-CN / en-US / de-DE），用户输入（标题、昵称）做 HTML 转义
  - `package.json` 的 `koishi.service` 声明 `optional: ["puppeteer"]`
- **开奖提醒改为控制台配置（按剩余时长分档 + 百分比定位）**：新增 `remind.rules` 表格，
  每行 = 「时长上限 `maxDuration`」+「提醒位置 `percent`（剩余时长的百分比）」；
  `maxDuration` 支持 `30m` / `1h` / `5h` / `1d` / `7d`（纯数字 = 分钟），`0` 或留空 = 不限（兜底行）；
  `percent` 填 1~99，同一行写多个（`20,10`）即多次提醒
  - **一个抽奖只命中一行**（上限最小且不小于自身时长的那行）→ **默认每场只提醒一次**，
    提醒时机随时长自动缩放：1 小时场次开奖前 12 分钟提醒、5 小时场次开奖前 45 分钟、7 天场次开奖前约 16.8 小时
  - 默认 4 行：`1h→20` / `5h→15` / `1d→10` / `0→10`；到点在抽奖所在的全部频道播报（倒计时按频道语言本地化）
  - 配置写错的行（时长识别不出、百分比为空或越界）会在启动时打一条 warn 并忽略，不影响其他行

### Removed 移除

- **手动提醒器交互全部下线（连同整套旧实现）**：`创建提醒器` / `提醒器列表` / `启用提醒器` / `禁用提醒器` /
  `删除提醒器` / `抽奖提醒` 六条指令不再注册（指令一览里也不再出现），提醒统一由控制台 `remind.rules` 配置
  - 删除对应的 6 个指令文件、4 个监听器（`reminder*` / `remind-add` / `remind-delete`）、
    3 个提醒消息构造函数与空壳 `util/reminder.ts`；`remind-add` / `remind-delete` / `reminder-add` /
    `reminder-delete` 四个事件随之取消
  - 数据模型去掉 4 张旧表（`reminder` / `remind` / `remind_channel` / `user_reminder`）的声明；
    **已存在的表不会被删除**，只是插件不再读写
  - 三语文案同步清掉这些指令的键（不留死翻译），包体从 189 KB 降到 171 KB

### Fixed 修复

- **开奖时间 4 段写法解析成 Invalid Date**：模板示例是 `09-15-20-00`（月-日-时-分），
  但解析函数按位置把第一段当「年」，于是算出 `year=9, month=15` → Invalid Date，
  抽奖会带着无效的自动开奖时间创建（自动开奖直接失效）。现在两种写法都支持：
  4 段 = 月-日-时-分（取当前年）、5 段 = 年-月-日-时-分；非法日期会明确报「时间格式不对」而不是静默接受。
- **提醒任务随抽奖生命周期清理**：手动 / 自动开奖、`删除抽奖`、记录过期清理时都会取消该抽奖已排定的开奖提醒，
  不会出现「抽奖已经开完 / 已经被删，提醒还在播报」；播报前再兜一层：抽奖记录不存在或已开奖直接返回，
  取不到频道配置时回退到全局语言，不再抛错

### Changed 变更

- **新增 `render.style` 风格选项**：`default`（Koishi 品牌蓝紫渐变）、`anime`（二次元：樱花粉 / 薰衣草紫渐变、
  圆润描边、💗/🤍 状态表情、🎁 奖品胶囊与 ✨🌸 点缀）、`gothic`（哥特：暗夜黑 + 血红 + 古金、衬线字体、
  金色细描边、❖/✦ 纹章装饰与罗马数字名次）、`avemujica`（Ave Mujica：暗紫黑 + 玫红 + 哥特金、🎭/🌹 点缀、
  「AVE MUJICA」字样与罗马数字名次），四套主题共用同一份 DOM，只切换主题 CSS
- **新增 `render.banner` 头图**：可给卡片顶部加一张横幅图（http(s) / `data:` / `file:` URL 或本机绝对路径，
  自动拼 file://），四种风格通用、留空不显示；插件不内置任何第三方美术素材
- **图片渲染 UI 重做**：三张卡片（创建 / 列表 / 开奖结果）统一为「顶栏（eyebrow + 标题 + 信息胶囊）+ 正文 + 页脚」结构，
  并引入一套设计变量（配色 / 圆角 / 阴影 / 字号 / 字体栈）：
  - 状态改成语义胶囊（带状态点），抽奖编号用等宽字体，加入口令高亮成虚线圈
  - 中奖者加名次奖牌（🥇🥈🥉）、头像圆环 + 昵称 + 等宽 QQ 号，奖品胶囊右对齐
  - 更细腻的背景光晕、卡片投影与分区小标题（带渐变竖条）

## [0.1.1] - 2026-09-14

**首个公开发布到 npm 的版本**（`0.1.0` 仅用于本地开发，未发布、未打 tag）。基于上游 [Roll-Bot-Project/roll-bot](https://github.com/Roll-Bot-Project/roll-bot) v1.3.0（commit `6ce60ab`）二次开发，**完整保留上游提交历史**（分叉点见仓库 tag `upstream-fork-point`）。

### Added 新增

- **参与条件**（`join` 配置组，在「发送加入口令」与 `giveaway.join` 两条路径上统一生效）
  - 群聊等级下限 `minGroupLevel`（QQ 群聊等级 1~100）
  - 最近发言 `minActiveDays`（要求最近 N 天内在本群发过言）
  - 最长连续发言天数 `minContinuousDays`（基于荣誉数据的 `day_count_max`，可自定义门槛，
    与官方「群聊之火 7 天 / 群聊炽焰 30 天」同源但更灵活）
  - QQ 群互动标识 `requiredHonors`（群聊之火 / 群聊炽焰 / 龙王）与 `honorMode`（满足任一 / 必须全部）；
    龙王的判定口径可用 `dragonScope` 在「昨日活跃榜」与「仅当前龙王」之间切换
  - 取不到数据时的策略 `onFetchError`（放行 / 拒绝）与缓存时长 `cacheMinutes`
  - 拒绝时会说明具体原因（当前等级、上次发言距今天数、缺少哪个标识）
- **荣誉数据自取能力**（`src/util/honorProvider.ts`）
  - 通过 OneBot 的 `get_cookies` 动作获取 `qun.qq.com` 登录态，改调 QQ 新版网页接口：
    `honor_talkative`（龙王/活跃榜）、`honor_continuous`（`continuous_type` 2=群聊之火、3=群聊炽焰）、`honor_emotion`
  - 自动计算 CSRF 令牌 `bkn`，并按 `cookie-only → bkn(skey) → bkn(p_skey) → bkn+g_tk → bkn+Origin`
    顺序探测可用鉴权方式（命中即缓存，避免反复试错）
  - 归一化成 OneBot 的 honor 结构，同时保留 `day_count` / `day_count_max` 等更细的字段
    （支持「最长连续发言 ≥ N 天」这类自定义门槛）
- **每个抽奖可单独设置参与条件（per-roll 覆盖）**
  - 创建模板新增「参与条件」一行，**直接带上控制台当前的限制条件**，创建者可以：
    ① 不改动 → 沿用控制台配置；② 删空 → 这个抽奖不限制；③ 改写 → 自定义门槛
  - 文本形态为固定 token：`等级≥40 活跃≥1 连续≥7 标识=群聊炽焰,龙王`（不需要的项删掉即可），
    中/英/德三种标签都能解析；**文本表达不了的口径**（多标识判定 `honorMode`、龙王口径 `dragonScope`）
    只在控制台配置、对所有抽奖生效，不会被某个抽奖创建时的快照顶掉
  - 与全局等价的策略**不落库**（继续跟随控制台），有差异才写入新表 `roll_policy`；
    加入抽奖时 per-roll 策略优先于全局配置
  - **`抽奖详情` 里显示当前生效的参与条件**（per-roll 优先，其次控制台配置；无限制时显示「不限」），
    参加者被拒之前就能看到门槛
- **管理员诊断指令**：`giveaway.debug.honor`（荣誉接口）、`giveaway.debug.member`（群成员等级/发言），
  两者都会把原始返回写进插件日志，便于线上排查
- **独立构建**：`npm run build`（`scripts/build.mjs`，esbuild + js-yaml，仅依赖 devDependencies），
  另有 `build:types`（tsc 生成 `.d.ts`）、`build:watch`、`clean`、`prepublishOnly`
- 提交 `package-lock.json` 固定构建工具链版本

### Changed 变更

- **创建抽奖交互重做为「复制模板填写」**
  - `创建抽奖` 会发一份**文字表格模板**（`奖品：` / `开奖时间：` / `加入口令：` / `标题：` / `描述：`），
    用户复制后逐项填写发回，**一次往返**完成
  - **留空的项按默认处理**（开奖时间留空 = 不自动开奖、口令留空 = 不用口令、标题/描述留空 = 默认值），只有奖品必填
  - 解析**按标签**取值并识别中/英/德三种标签与全角冒号；群里随口一句（没有标签）不会被误当成奖品，
    整段复制粘贴也可用（说明行被忽略）
  - 缺奖品、开奖时间不合法、回复 `q` / `取消`、或不回复 → **直接取消创建**，不再逐步追问
  - 位置参数写法保留（有参数时输出模板的步骤被跳过）：`抽奖 add 显卡*1 09-15-20-00 参加`；
    标题/描述/中奖人可重复可用 `-t` / `-d` / `-r`，模板里填的值优先
  - 新增 `parseCreateForm` 纯函数（标签解析 + 格式校验，可单测）
  - 模板改为**纯文本**（去掉 `<p>` / `<b>` 等元素标签），保证复制粘贴出来是干净内容
  - 口令不为空时，创建成功的提示会带上口令；**口令为空时不再参与关键词匹配**
    （避免内容为空的消息误命中无口令抽奖）
- **开奖消息 @ 中奖者**：中奖行的昵称改成真正的 `<at>` 元素（QQ 里是会被提醒的 @），后面保留 `(QQ号)` 便于留档；
  中奖者已退群、或取不到昵称时（`bot.getUser` 返回空）不会再让整条开奖消息发不出去；
  顺带补齐了 en-US / de-DE 的开奖文案（此前是中文残留）
- **`抽奖` 直接输出指令一览**（此前会落到框架 help 插件的通用帮助，列的是注册名 `giveaway delete` 这种英文）
  - 根指令带上 action 后由插件自己输出，按「参与 / 创建与管理 / 设置 / 管理员」分组；
    名字按当前语言取（**中文环境用中文别名**，en/de 用注册名），描述仍取自各指令自己的 i18n 文案
  - 管理员分组（`抽奖接口诊断` / `抽奖成员诊断`，后者是新增别名）只对有管理权限的人显示；
    `抽奖帮助` 输出同一份表并附文档链接
  - 测试断言「指令表覆盖全部可执行指令」，以后新增指令忘记补分组会直接失败
- 包名 / 插件名 / 指令根：`koishi-plugin-roll-bot` → **`koishi-plugin-giveaway`**；
  指令根由 `roll` + `remind` 两个根统一到 `giveaway`（`remind.*` → `giveaway.reminder.*`）；
  事件总线前缀 `roll-bot/*` → `giveaway/*`
- **README 重构**：改为「特性 / 安装 / 指令列表（含完整指令-别名-权限表）/ 配置 / 事件 / 开发」结构，
  上游信息只保留开头一个链接（本仓库作为独立插件说明），不再逐条罗列与上游的差异
- **配置界面重写**为四组：`basic`（记录保留时长、默认时区）、`permission`（权限）、`join`（参与条件）、
  `remind`（提醒器）；三语文案全部重写（en/de 由上游的中文残留改为真正的英文 / 德文）
- **en-US / de-DE 语言包补齐**：上游遗留的 125 条中文文案（命令回复、事件提示、消息模板）全部翻译为英文 / 德文，
  连半翻译的混杂行（如 `Creation cancelled; just send "创建抽奖"`）也改成引用英文指令名；
  三套语言包现在 **key / `{占位符}` / 元素标签完全一致、en/de 零中文残留**，由离线用例持续校验
- **权限模型改接 Koishi 原生等级**（用户表 `authority` 字段：0 封禁 / 1 普通用户 / 3 管理员 / 4 超级管理员），
  新增 `authorityCreate`（创建抽奖所需等级）与 `authorityManage`（管理他人抽奖所需等级）

### Fixed 修复

- **群管理员判定失效**：OneBot 适配器给出的 `member.roles` 是 `[{ id: 'admin' }]` 对象数组，
  而上游直接拿 `roles[0]` 与字符串比较，导致「群主/群管理员可管理本群抽奖」**恒不生效**；
  现已兼容对象数组与字符串数组两种形式
- 拿不到群成员角色信息时（私聊、未知平台）不再一律放行
- `requiredHonors` 中被清空后残留的 `null` 会被当成有效条件（导致条件永远生效、全员被拒），现已过滤无效值；
  启动时若发现控制台配置里有空项/错值会打印告警，并说明当前实际生效的标识
- **控制台里「互动标识」的空行会伪装成「群聊之火」**：schemastery-vue 判断选项是否匹配用的是
  `optional(schema)(null)`，而非 required 的 `const` 会接受 `null`，于是空行在界面上显示成第一个选项
  （群聊之火），实际存进配置的却是 `null` —— 表现就是「控制台明明选了三个，实际只生效两个」。
  现在三个候选项都是 `required`，空行在界面上显示为空；历史配置里的 `null` 仍能正常解析
  （插件按无效值忽略，并在启动时告警提示去控制台删掉空行）
- `onFetchError: deny` 拒绝时补上告警日志（此前拒绝分支没有任何日志，线上排查只能靠上游日志）
- 荣誉接口返回空列表时不再直接判成「你没有标识」，而是按「取不到数据」处理

### Removed 移除

- 单字母别名 `r` / `rd` / `d`（空前缀配置下极易误触，且 `r` 与骰子类插件冲突）
- `-n` 快速创建选项（语义被"只回一行、开奖时间填 `n`"取代）
- 旧文档配置组 `usage`、`basic.adminUsers`、`permission.allowNormalUserAdd`（分别由文档链接、Koishi 权限等级、
  `permission.authorityCreate` 取代）

### 说明

- NapCat 自带的 `get_group_honor_info` 自 QQ 荣誉页改版（改为 Vite SPA，`window.__INITIAL_STATE__` 不复存在）
  后失效，上游 v4.18.19 仍未修复 → 本插件的荣誉取数**不再依赖它**；
  先用 OneBot 标准接口（将来修好会自动优先使用），取不到时自动切换到新版网页接口
- 未配置任何参与条件时**零开销**：不发起任何接口调用；非 OneBot 平台会跳过参与条件检查

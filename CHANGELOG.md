# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
0.x 开发版的变更历史见 [`CHANGELOG-0.x.md`](./CHANGELOG-0.x.md)。

## [1.0.1] - 2026-09-17

> 1.0.0 只发布到 GitHub（未发 npm），1.0.1 是第一个发布到 npm 的版本。

### Fixed 修复

- **`roll_member` 的唯一索引在已有数据库上没有真正建出来**：1.0.0 用 `unique: [['roll_id','user_id']]` 声明，
  而该写法**只在新建表时**写入表级约束，已经存在的表不会被补（实测线上库 `roll_member` 上没有任何索引）。
  改为用 `indexes: [{ keys: {...}, unique: true }]` 声明 —— 驱动会在**表首次被访问时补建唯一索引**，
  老库同样生效。已用线上库副本验证：14 行数据保留、索引建出、重复插入被数据库拦下
  （minato 的 `IndexDef` 类型漏了 `unique` 字段，代码里按运行时能力做了类型规避并注释说明）
  > 若历史数据里已存在 `(roll_id, user_id)` 重复行，建索引会失败（插件仍可运行，只是少了这层保护）。
  > 清理：`DELETE FROM roll_member WHERE id NOT IN (SELECT MIN(id) FROM roll_member GROUP BY roll_id, user_id);`

## [1.0.0] - 2026-09-17

首个正式版本。1.0 起按「全新开始」对待：**不承诺与 0.x 的配置 / 数据结构兼容**，请按 README 重新配置；
此后只做缺陷修复与社区需求，功能面保持稳定（详见 README 的「维护状态」）。

### 功能

- **创建抽奖**：`创建抽奖` 发一份纯文本模板（含当前控制台的参与条件），复制填写后一次往返完成；
  留空即默认、按标签解析（中 / 英 / 德标签、全角冒号、整段粘贴均可）；也支持位置参数写法与 `-t` / `-d` / `-r` 选项
- **参与条件**：群聊等级下限、最近 N 天发过言、最长连续发言天数、QQ 群互动标识（群聊之火 / 群聊炽焰 / 龙王，
  可选「满足任一 / 必须全部」，龙王口径可切「昨日活跃榜 / 仅当前龙王」），取不到数据时按 `onFetchError` 放行或拒绝；
  **每个抽奖可单独覆盖**能文本表达的条件，详情里显示实际生效的门槛
- **参与与退出**：群内发送加入口令，或 `加入抽奖 <编号>`；`退出抽奖 <编号>`；开奖后名单固定，不能再加入或退出
- **开奖**：创建时填了开奖时间即到点自动开奖，也可手动 `开奖 [编号]`；结果广播到抽奖所在的全部频道，
  含一条真实 `@中奖者` 的文字行与结果图片；参与人数少于奖品数且未开启重复中奖时，只抽出与参与人数相同的名额
- **开奖提醒**：控制台按「创建时距开奖的剩余时长」分档配置规则表（时长上限 + 剩余时长百分比），
  一个抽奖只命中一行、默认每场只提醒一次；任务随开奖 / 删除 / 过期自动清理
- **查询**：`抽奖列表`（支持跨频道）、`抽奖详情 <编号>`（含参与名单与中奖名单）、`抽奖成员 <编号>`
- **图片卡片（可选）**：装上 `koishi-plugin-puppeteer` 后，创建结果 / 详情 / 参与名单 / 列表 / 开奖结果都可出图，
  三套主题（`default` / `anime` / `avemujica`）、可配头图与卡片宽度、参与名单可限显示人数；未装或渲染失败自动回退文字
- **权限**：沿用 Koishi 原生权限等级（0 / 1 / 3 / 4），创建者始终可管理自己的抽奖，群主与群管理员可开关删除 / 手动开奖
- **多语言与多时区**：简体中文 / English / Deutsch 三语覆盖群消息、指令一览、控制台配置说明、图片卡片标签与诊断输出；
  开奖时间按用户 / 频道 / 默认时区逐级解析
- **诊断指令**：`抽奖接口诊断` / `抽奖成员诊断`（管理员）逐个打印荣誉与群成员接口的可用性，原始返回写入插件日志
- **生命周期事件**：`giveaway/roll-add`、`roll-end`、`roll-expired`、`roll-join`、`roll-quit`、`roll-key-update`、`remind-broadcast`

### 相对 0.1.3 的修复

- 加入 / 退出抽奖用不存在的编号、或用户没有 binding 时不再抛内部异常，改为给出明确提示
- 已开奖的抽奖不再能被加入，也不再允许退出（口令与指令两条路径统一拦截）
- 参与人数少于奖品数时，「不允许重复中奖」不再静默失效（只抽 `min(参与人数, 奖品数)` 个名额）
- `roll_member` 增加 `(roll_id, user_id)` 唯一索引，重复发口令不会再写入两条参与记录
- 开奖时间填成过去的时间会被拒绝（此前会导致该抽奖永远不会自动开奖，且删除时异常中断清理流程）
- 抽奖记录过期时补删 `roll_policy`；4 段式开奖时间改用目标时区年份；管理员诊断输出改为三语

### 开发者

- 仓库内 6 套离线用例（`npm test`，391 条断言：创建流程 / 参与条件 / 控制台表单 / 权限 / 三语 / 交互边界）
- CI（GitHub Actions）：Node 20 / 22 上跑 `npm ci → npm run build → npm test`
- 可选的像素级验收 `npm run test:shots`（真实 Chromium 截图，需 `puppeteer-core` + `CHROME`）
- 发版步骤与冒烟清单见 [`docs/release-checklist.md`](./docs/release-checklist.md)
